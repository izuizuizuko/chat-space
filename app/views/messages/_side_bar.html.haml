%l
  %body
    .sidebar
      .sidebar__username
        .sidebar__username_box
          .sidebar__userename_box_name
            = current_user.name
          .sidebar__username_box_menu
            .sidebar__username_box_menu_edit-user
              = link_to new_group_path do
                = icon('fas fa-edit',  class: "contents__btn--icon")
            .sidebar__username_box_menu_newgroup
              = link_to edit_user_path(current_user) do
                = icon('fas fa-cog',  class: "contents__btn--icon")


      .sidebar__list
        - current_user.groups.each do |group|
          .sidebar__list_box
            = link_to '#' do
              .sidebar__list_box_name
                = group.name
              .sidebar__list_box_lastmessage
                メッセージはまだありません。